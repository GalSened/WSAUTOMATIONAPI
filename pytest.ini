[pytest]
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning


markers =
    run: mark a test as a run.

log_file = AutomationLog.log
log_file_level = INFO
log_file_format = %(asctime)s %(levelname)s %(filename)s:%(lineno)d %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S
